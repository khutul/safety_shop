# safety_shop — Кодын аудит ба цэвэрлэгээний төлөвлөгөө (2026-08-24)

## 1. Storefront (Next.js) — илэрсэн асуудлууд

### 1.1 Эвдэрхий (Medusa SDK дуудсан хэвээр) хуудсууд — ЧУХАЛ
Эдгээр route-ууд устгагдсан Medusa backend руу хандахыг оролддог тул нээвэл алдаа өгнө:

| Route | Асуудал |
|---|---|
| `/account/*` (бүх дэд хуудас) | `customer.ts`, `orders.ts` → Medusa SDK. **Side-menu-д «Миний хаяг» линк идэвхтэй байгаа** тул хэрэглэгч дарж орох боломжтой! |
| `/checkout` | Хуучин Medusa checkout. Бодит худалдан авалт нь `/cart` (manada-cart) дээр явагддаг тул давхардсан, эвдэрхий |
| `/order/[id]/*` (confirmed, transfer) | Medusa order API |
| `/collections/[handle]` | Medusa collections |

**Ажиллаж байгаа урсгал:** каталог (products, categories, brands, industries, hero, settings) → Odoo `/api/v1`; сагс → localStorage; захиалга → `POST /api/v1/orders`; төлбөр → QPay QR; шалгах → `/order-status`.

### 1.2 Үхмэл код (Medusa үлдэгдэл)
- `lib/data/`: `cart.ts`, `customer.ts`, `orders.ts`, `fulfillment.ts`, `payment.ts`, `collections.ts`, `variants.ts` — бүгд Medusa SDK
- `lib/config.ts` (sdk), `lib/util/medusa-error.ts`
- `modules/account`, `modules/checkout` (ихэнх нь), `modules/order` (transfer хэсэг)
- `package.json`: `@medusajs/js-sdk`, `@medusajs/icons`, `@medusajs/ui-preset`, `@medusajs/types` хамаарлууд

**Санал:** Богино хугацаанд — side-menu-гээс `/account` линкийг авах. Дунд хугацаанд — account/login-ийг Odoo дээр шинээр хийхээр шийдсэн үед эдгээр route/код устгах эсвэл дахин бичих.

## 2. Odoo модулиуд (safety_catalog, safety_api)

### 2.1 Сайн талууд
- Headless зарчим баригдсан: бүх тооцоо (үнэ, НӨАТ, pricelist, нөөц) Odoo талд
- N+1-ээс зайлсхийсэн (`read_group`), кэш header, rate limit, QPay callback-ийг дахин шалгадаг (итгэдэггүй) — зөв
- Захиалга үүсгэхэд нөөц шалгаж, backorder зөвшөөрөл асуудаг

### 2.2 Анхаарах зүйлс
1. **Нэршил PROJECT_RULES-аас зөрсөн.** Дүрэмд `safety_shop_<concern>` / `safety_shop.*` гэсэн ч бодит нь `safety_catalog`, `safety_api` / `safety.catalog.*`. Одоо нэр солих нь өгөгдөл эвдэх эрсдэлтэй тул **PROJECT_RULES-ыг бодит нэршилд тааруулж шинэчлэхийг** санал болгоно.
2. **Access rights сул:** `product_image/document/feature`, `stock_count` дээр `base.group_user` бүрэн эрхтэй (write/create/unlink). Least-privilege болгож manager түвшинд шилжүүлэх нь зөв.
3. **CORS `*`:** Бүх endpoint `cors="*"`. Production дээр `https://manada.mn`-ээр хязгаарлах.
4. **Алдааны мэдээлэл задарч байна:** `create_order` дээр `str(e)`-г шууд клиент рүү буцаадаг (`partner_failed`, `order_failed`) — дотоод мэдээлэл задрах эрсдэл. Ерөнхий мессеж буцааж, дэлгэрэнгүйг log-д бичих.
5. `__pycache__`/`.pyc` файлууд addons дотор хуримтлагдсан (git-д ороогүй ч цэвэрлэвэл зүгээр).
6. `main.py` дотор `import json` функц дотор давхардсан — модулийн түвшинд гаргах (жижиг).

## 3. Устгах/цэгцлэх нэр дэвшигчид

| Зам | Хэмжээ | Тайлбар | Санал |
|---|---|---|---|
| `_archive/` | 251 MB | Хуучин төслүүд (HAB web, medusa_bak, old_static_site…) | Устгах (эсвэл repo-ноос гадагш нөөцлөх) |
| `apps/safety.mn/` | 246 MB | Хуучин статик HTML mockup, deploy-д ашиглагддаггүй | Устгах |
| `apps/backend/` | 55 MB | Medusa backend — ашиглагдахгүй, root `package.json`-д script үлдсэн | Устгах + script цэвэрлэх |
| `web zurag/` | 28 MB | Git-д орсон ч storefront ашигладаггүй зургууд | Устгах эсвэл нөөц рүү |
| `_to_delete/` | 5.6 MB | Нэр нь хэлээд байна | Устгах |
| `зураг дэлгүүр/` | 0 | Өнөөдөр үүссэн хоосон фолдер | Зориулалтыг асуух |
| `odoo/filestore.tgz`, `safety_shop.dump`, `filestore_safety_shop/` | 135 MB | Нөөц файлууд (git-д ороогүй — зөв) | Repo-ноос гадуур нөөцийн фолдерт зөөх |
| `README.md` (root) | — | Medusa DTC starter-ийн README хэвээр | Төслийн бодит README болгож солих |

**Нийт чөлөөлөгдөх зай:** ойролцоогоор 700+ MB.

## 4. Бусад ажиглалт
- Root `package.json` / `turbo.json` нь Medusa monorepo бүтэцтэй — backend устгасны дараа хялбарчлах
- Git working tree дээр олон файл «modified» харагдаж байгаа нь permission/line-ending асуудал байж болзошгүй; sandbox-оос git commit хийхэд `.git/index.lock` дээр permission алдаа гарсан тул git үйлдлүүдийг Windows талаас хийх нь найдвартай

## 5. Санал болгох дараалал
1. Хоосон фолдер, `_to_delete`, `_archive`, `apps/safety.mn`, `web zurag` устгах *(зөвшөөрөл шаардлагатай)*
2. `apps/backend` устгаж root package.json/turbo.json цэвэрлэх
3. Side-menu-гээс `/account` линк авах (эвдэрхий хуудас руу орохоос сэргийлнэ)
4. Odoo засварууд: access rights чангалах, CORS хязгаарлах, алдааны мессеж нуух
5. PROJECT_RULES-ын нэршлийн хэсгийг бодит байдалд тааруулах, root README шинэчлэх
6. Дараа нь: storefront-ын Medusa route/код устгах (account шийдвэртэй уялдуулж)
